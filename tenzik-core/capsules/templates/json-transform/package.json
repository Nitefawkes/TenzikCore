{
  "name": "tenzik-json-transform",
  "version": "0.1.0",
  "description": "JSON transformation capsule for Tenzik with verifiable execution",
  "main": "capsule.ts",
  "scripts": {
    "build": "asc capsule.ts --outFile build/capsule.wasm --optimize --runtime stub --exportRuntime --memoryBase 0",
    "build:optimized": "npm run build && wasm-opt build/capsule.wasm -Oz -o build/capsule.wasm",
    "test": "node test.js"
  },
  "keywords": [
    "tenzik",
    "wasm",
    "json",
    "transform",
    "verifiable"
  ],
  "author": "Tenzik Core Contributors",
  "license": "Apache-2.0",
  "devDependencies": {
    "assemblyscript": "^0.27.0",
    "wasm-opt": "^1.3.0"
  }
}
